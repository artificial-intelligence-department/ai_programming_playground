/*Критерії приймання коду:

в коді використана як мінімум одна цілочисельна змінна ^
в коді використана як мінімум одна дійсна змінна ^
в коді використана як мінімум одна дійсний з подвійною точністю змінна ^
в коді використана як мінімум одна цілочисельна константа ^
в коді використані умовні оператори та розгалуження ^
в коді використаний одновимірний масив ^
в коді використаний двовимірний масив 
в коді використаний do while цикл
в коді використаний while цикл
в коді використаний for цикл ^
в коді використано оператори break і continue
в коді використано перевантаження функції 
в коді використано рекурсивну функцію 
в коді використано параметри та аргументи функції	
в коді використано математичні операції та математичні функції
в коді використано функції роботи з файлами, для того, щоб записати у файл
в коді використано функції роботи з файлами, для того, щоб зчитати з файлу
в коді використано оператори виведення та введення даних
в коді використано свою структуру даних
в коді використано вказівники
*/
/*Загальні відомості
В американській системі виміру довжин використовуються ярди, фути, дюйми. 1 Ярд = 3 фути. 1 Фут = 12 дюймів. 1 Дюйм = 2.54 см.
Футбольні поля мають можуть мати різну довжину
Довжина: мінімум 100 ярдів, максимум 130 ярдів.
Ширина: мінімум 50 ярдів, максимум 100 ярдів.

// Довжина мого поля 110 ярдів

Використовуючи Ваше прізвище розрахувати найменшу довжину, яка необхідна для розміщення банера з Вашим прізвищем.
 За умови, що прізвище можна записати лише в 1 ряд, а ширина букви(разом з інтервалом між символами) = кількості голосних букв у Вашому прізвищі * 0.8 метра.
На основі обчислених розмірів букви визначити символи, які будуть знаходитись на 3-х центральних(по довжині) метрах поля. 
Обчислення зробити для 2-х випадків:
1) при написанні прізвища з лівого краю поля —>  до правого
2) при написанні прізвища з правого краю поля <—  до лівого.
Результат записати.
*/
#include <iostream>
#include <string>
#include <cmath>

using namespace std;

// цілочисельна змінна
int width; // ширина букви
// дійсна змінна
float length; // довжина банера
// дійсний з подвійною точністю змінна
double inch_in_meter = 0.0254; // дюйм в метрах
// цілочисельна константа
const int stadium = 110;

// Одновимірний масив
string surname; // прізвище

int main() {
  // Введення прізвища
  cout << "Enter your last name: ";
  cin >> surname;

  // Розрахунок ширини букви
  for (char c : surname) {
    width += (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u'|| c == 'A'|| c == 'E'|| c == 'I'|| c == 'O'|| c == 'U') ? 0.8 : 0.5;
  }
  cout<<width<<endl;
  // Розрахунок довжини банера
  length = width * surname.length();
  cout << length<<endl;
  // Виведення довжини банера
  cout << "The length of the banner: " << length << " meters" << endl;

  // Визначення символів, які будуть знаходитись на 3-х центральних(по довжині) метрах поля
  // 1) при написанні прізвища з лівого краю поля —>  до правого
  int start_index = (int) (length / 2) - 2 * width;
  int end_index = (int) (length / 2) + 2 * width;
  for (int i = start_index; i < end_index; i++) {
    cout << surname[i];
  }
  cout << endl;
  // 2) при написанні прізвища з правого краю поля <—  до лівого
  start_index = (int) (length / 2) + 2 * width;
  end_index = (int) (length / 2) - 2 * width;
  for (int i = start_index; i >= end_index; i--) {
    cout << surname[i];
  }
  cout << endl;

  return 0;
}